apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tu-evento-backend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - api.tuevento.com
    secretName: tuevento-tls
  rules:
  - host: api.tuevento.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tu-evento-backend-service
            port:
              number: 80
