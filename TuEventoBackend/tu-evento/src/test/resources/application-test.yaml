spring:
  datasource:
    url: jdbc:h2:mem:testdb;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  liquibase:
    enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
      - org.springframework.session.data.redis.config.annotation.web.http.RedisHttpSessionConfiguration
      - org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

app:
  jwt:
    secret: test-secret-key-for-jenkins-ci-only-32chars
    access-token-expiration: 900000
    refresh-token-expiration: 604800000
  mail:
    from: test@test.com
  cors:
    allowed-origins: "http://localhost:3000"
  storage:
    enabled: false
  admin:
    email: admin@test.com
    password: admin123
    alias: admin
  oauth:
    google:
      client-id: test
      client-secret: test
      redirect-uri: http://localhost:8080/api/v1/auth/oauth/google/callback
      token-uri: https://oauth2.googleapis.com/token
      userinfo-uri: https://www.googleapis.com/oauth2/v3/userinfo
      authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
    facebook:
      client-id: test
      client-secret: test
      redirect-uri: http://localhost:8080/api/v1/auth/oauth/facebook/callback
      auth-uri: https://www.facebook.com/v18.0/dialog/oauth
      token-uri: https://graph.facebook.com/v18.0/oauth/access_token
      profile-uri: https://graph.facebook.com/me
  profile:
    default-avatar-stored-file-id: 1

server:
  port: 0